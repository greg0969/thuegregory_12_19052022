
export const urlMocked = {

    userMainData(id) {
        
        return `../mocked/User${id}/USER_MAIN_DATA.json`
        
    },

    userActivity(id) {
        return `../mocked/User${id}/USER_ACTIVITY.json`
    },

    userAverageSession(id) {
        return `../mocked/User${id}/USER_AVERAGE_SESSIONS.json`
    },

    userPerf(id) {
        return `../mocked/User${id}/USER_PERFORMANCE.json`
    }
    
}